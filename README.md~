# viralcents

1) install foreman on remote server

2) install redis on remote server

3) intall rmagick/minimagick on remote server

4) replace the apis credentials everywhere (mail/amazon/message)


